
Функция СформироватьСписокМетодов() Экспорт
	
	СписокМетодов = Новый Массив();
	Макет = ПолучитьМакет("Методы");

	Для НомерСтроки = 2 По Макет.ВысотаТаблицы Цикл
		
		НаименованиеМетода = СокрЛП(Макет.Область(НомерСтроки, 1).Текст);
		СписокМетодов.Добавить(НаименованиеМетода);
	КонецЦикла; 
	
	Возврат СписокМетодов; 
КонецФункции 
