
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Продукт") Тогда
		Список.Параметры.УстановитьЗначениеПараметра("Значение", Параметры.Продукт);
		
		ЭтаФорма.АвтоЗаголовок = Ложь;
		ЭтаФорма.Заголовок = "Документы по продукту: "+ Параметры.Продукт;
	КонецЕсли;
	
КонецПроцедуры
