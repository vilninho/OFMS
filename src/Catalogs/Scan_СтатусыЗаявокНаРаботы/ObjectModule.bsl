Процедура ПередЗаписью(Отказ)
	
	Если ЭтоНовый() Тогда
		Запрос = Новый Запрос("ВЫБРАТЬ Наименование ИЗ Справочник.Scan_СтатусыЗаявокНаРаботы ГДЕ Наименование = &Наименование ");
       Запрос.УстановитьПараметр("Наименование",Наименование);
       Результат = Запрос.Выполнить();
       Если НЕ Результат.Пустой() Тогда
          //rarus FominskiyAS 21.04.2019  mantis 14191 +++
		  //Сообщить("Элемент с таким наименованием уже есть");
		   //rarus bonmak 20.12.2019 15531 ++
		  //Сообщить(НСтр("ru = 'Элемент с таким наименованием уже есть'; en = 'Element with the same name already exists'"));
		  ТекстОшибки = НСтр("ru = 'Элемент с таким наименованием уже есть'; en = 'Element with the same name already exists'"); 
		  Сообщить (ТекстОшибки);
		  ЗаписьЖурналаРегистрации("Ошибка справочника", УровеньЖурналаРегистрации.Ошибка,, Ссылка, ТекстОшибки); 
		  //rarus bonmak 20.12.2019 15531 --

		  //rarus FominskiyAS 21.04.2019  mantis 14191 ---  
		  Отказ = Истина;
		КонецЕсли;            
	КонецЕсли;
 
КонецПроцедуры 